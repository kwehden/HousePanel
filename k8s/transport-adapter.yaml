apiVersion: apps/v1
kind: Deployment
metadata:
  name: housepanel-transport-adapter
  namespace: housepanel
  labels:
    app: housepanel-transport-adapter
    app.kubernetes.io/part-of: housepanel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: housepanel-transport-adapter
  template:
    metadata:
      labels:
        app: housepanel-transport-adapter
    spec:
      serviceAccountName: housepanel-transport-adapter
      automountServiceAccountToken: false
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/arch
                    operator: In
                    values: ["amd64"]
      containers:
        - name: transport-adapter
          image: laminarflow:30500/housepanel/transport-adapter:0.1.12
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8002
          env:
            - name: PYTHONPATH
              value: "/app/src"
            - name: AGGREGATOR_URL
              value: "http://housepanel-aggregator:8001"
            - name: LOG_LEVEL
              value: "INFO"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8002
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /internal/health
              port: 8002
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: housepanel-transport-adapter
  namespace: housepanel
  labels:
    app.kubernetes.io/part-of: housepanel
    housepanel.io/metrics: "true"
spec:
  type: NodePort
  selector:
    app: housepanel-transport-adapter
  ports:
    - name: http
      port: 8002
      targetPort: 8002
      nodePort: 30802
