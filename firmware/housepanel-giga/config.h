#pragma once

// Local credentials and network config — defined in secrets.h (git-ignored).
// Copy secrets.h.example → secrets.h and fill in your values.
#include "secrets.h"

#define TRANSPORT_ADAPTER_WS_PATH "/ws/panel"
#define FIRMWARE_VERSION "0.3.9"
